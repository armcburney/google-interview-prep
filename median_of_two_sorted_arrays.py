def median(nums1, nums2):
    n = len(nums1)
    m = len(nums2)
    size = n + m

    # Partition the two arrays such that
    # len(left_a) = len(right_a) +- 1


def binary_search(arr, high, low, value):
    pass
